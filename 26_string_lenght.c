#include <stdio.h>
#include <string.h>

int main()
{
  char x[30];
  int len;

  printf("Enter a string to calculate its length\n");
  gets(a);

  len= strlen(x);

  printf("Length of the string = %d\n", len);

  return 0;
}
